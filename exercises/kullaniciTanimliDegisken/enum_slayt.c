#include<stdio.h>
/*
int main(void) {
    // define new data type boolean
    enum boolean {
        false = 0,
        true = 1
    };

    enum boolean isTrue;
    isTrue = true ;
    if(isTrue==true) {
        printf("True\n");
    }

    return 0;
}
*/
/*
int main(void) {
    // define new data type (dt)
    // Also define a new variable with the new dt
    enum boolean {
        false = 0,
        true = 1
    } isTrue;

    isTrue = true ;
    if(isTrue==true) {
        printf("True\n");
    }

    return 0;
}
*/
/*
int main() {
    // define new data type mainColors
    enum mainColors {
        red,
        blue,
        yellow
    };
    // define variable
    enum mainColors pixel;
    pixel=blue;

    if(pixel==red)
        printf("red pixel\n");
    else if (pixel==blue) 
        printf("blue pixel\n");
    else 
        printf("yellow pixel\n");
    return 0;
}
*/
